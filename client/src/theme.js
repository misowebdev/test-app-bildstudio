import { createTheme } from "@mui/material";

const theme = createTheme({
  palette: {
    primary: {
      main: "#2ecc71",
    },
    secondary: {
      main: "#00bbf5",
    },
  },
});

export default theme;
